// Entry point for Cherry's client-side islands. The first real island is the
// no-flash theme toggle (DO_NEXT slice 6); until then this keeps the TS
// toolchain honest by giving tsc and esbuild a real input.
export {};
