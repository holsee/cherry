# LF everywhere, on every OS. Formatting and byte-identical builds (ADR 0005)
# both depend on stable line endings across checkouts.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff2 binary
